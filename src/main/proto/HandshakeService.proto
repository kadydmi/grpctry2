syntax = "proto3";
option java_multiple_files = true;
package com.service.codegen;

message HandshakeRequest {
  string firstName = 1;
  string lastName = 2;
}

message HandshakeResponse {
  string greeting = 1;
}

service HandshakeService {
  rpc hello(HandshakeRequest) returns (HandshakeResponse);
}